{
  "name": "albania-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@albania/web",
    "dev:web": "npm run dev --workspace=@albania/web",
    "dev:mobile": "npm run start --workspace=@albania/mobile",
    "build": "npm run build --workspace=@albania/web",
    "build:web": "npm run build --workspace=@albania/web",
    "lint": "npm run lint --workspaces --if-present",
    "preview": "npm run preview --workspace=@albania/web",
    "supa": "supabase",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "supabase": "^2.72.8",
    "typescript": "^5.8.3"
  }
}
