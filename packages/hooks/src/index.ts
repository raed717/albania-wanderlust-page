// Shared Hooks (platform-agnostic, no web-specific dependencies)
export * from "./use-mobile";

// Note: use-toast and useSearchFilters remain in apps/web/src/hooks
// because they have dependencies on web-specific components/services
